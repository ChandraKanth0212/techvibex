"""Demo test-suite (Phase 7B end-to-end demo dataset).

A self-contained, deterministic, synthetic-only world that exercises the real
optimizer pipeline end to end. Nothing here touches live Indian Railways data —
every catalogue is fabricated by :mod:`demo_data` (the catalogue carries the
``SYNTHETIC_DEMO`` disclaimer).
"""
