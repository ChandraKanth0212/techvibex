# Demo data

Reserved for synthetic demo datasets (corridors, assets, tasks, block requests,
train movements, goods forecasts) used to exercise the optimisation pipeline in
demo mode. Populated in Phase 2 together with the sample runner in
`../scripts/`.